# POS System Dependencies
# Install with: pip install -r requirements.txt

# Password hashing
bcrypt>=4.0

# Thermal printer support (ESC/POS protocol)
python-escpos>=3.0

# Serial port support (for thermal printers)
pyserial>=3.5

# eBarimt POS SDK (Mongolian national e-receipt API v3.0)
ebarimt-pos-sdk>=0.3

# Image processing
Pillow>=10.0

# Testing
pytest>=7.0
