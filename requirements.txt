# POS System Dependencies
# Install with: pip install -r requirements.txt

# Web framework
Flask>=2.3.0

# Password hashing
bcrypt>=4.0

# Thermal printer support (ESC/POS protocol)
python-escpos>=3.0

# Serial port support (Windows COM ports, Linux serial)
pyserial>=3.5

# Desktop application wrapper (native window, no browser needed)
pywebview>=5.0

# Multi-monitor detection (auto-position customer display on second screen)
screeninfo>=0.8

# eBarimt POS SDK (Mongolian national e-receipt API v3.0)
ebarimt-pos-sdk>=0.3

# Build (optional — for creating standalone .exe / binary)
pyinstaller>=6.0
# Image processing (icon generation)
Pillow>=10.0

# Testing
pytest>=7.0
pytest-flask>=1.3
